import { ApplySkeleton } from "@/components/ui/page-skeletons";

/** Covers the job and Manatal field-id lookups the apply shell waits on. */
export default function Loading() {
  return <ApplySkeleton />;
}
